export const description = "Boolean (true/false) indicated whether the event is a certain type of patrol (e.g. overnight patrol == true)";

export default {
    type: "boolean",
    example: "true",
    description
};
